/* City Mate - Enhanced Sunset Travel Theme v2.0 */

/* CSS Variables */
:root {
    --color-bg-primary: linear-gradient(135deg, #ff7b00 0%, #d433ff 50%, #00d4ff 100%);
    --color-bg-secondary: linear-gradient(135deg, #ff9a00 0%, #a855f7 100%);
    --color-card: rgba(255, 255, 255, 0.95);
    --color-text-primary: #1e293b;
    --color-text-secondary: #64748b;
    --color-accent: #f97316;
    --color-success: #10b981;
    --color-danger: #ef4444;
    --color-purple: #8b5cf6;
    
    --glass-bg: rgba(255, 255, 255, 0.15);
    --glass-card: rgba(255, 255, 255, 0.95);
    --glass-border: rgba(255, 255, 255, 0.3);
    
    --spacing-xs: clamp(0.5rem, 2vw, 1rem);
    --spacing-sm: clamp(1rem, 4vw, 2rem);
    --spacing-md: clamp(2rem, 6vw, 4rem);
    --spacing-lg: clamp(4rem, 8vw, 6rem);
    --font-size-base: clamp(1rem, 2.5vw, 1.2rem);
    --font-size-lg: clamp(1.5rem, 4vw, 2rem);
    --font-size-xl: clamp(2.5rem, 7vw, 4rem);
    --border-radius: 28px;
    
    --shadow-sm: 0 10px 40px rgba(0,0,0,0.12);
    --shadow-md: 0 25px 80px rgba(0,0,0,0.18);
    --shadow-lg: 0 40px 100px rgba(0,0,0,0.25);
}

[data-theme="dark"] {
    --color-bg-primary: linear-gradient(135deg, #1e1b4b 0%, #3b82f6 50%, #06b6d4 100%);
    --color-card: rgba(31, 41, 55, 0.98);
    --color-text-primary: #f8fafc;
    --color-text-secondary: #cbd5e1;
    --glass-bg: rgba(255, 255, 255, 0.08);
    --glass-card: rgba(31, 41, 55, 0.98);
}

/* Base */
* { margin: 0; padding: 0; box-sizing: border-box; }

body {
    font-family: 'Poppins', sans-serif;
    line-height: 1.7;
    color: var(--color-text-primary);
    background: var(--color-bg-primary);
    min-height: 100vh;
    font-size: var(--font-size-base);
    overflow-x: hidden;
}

/* Logo Header - Perfect Alignment */
.logo-header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    background: var(--color-bg-secondary);
    backdrop-filter: blur(25px);
    z-index: 999;
    transition: all 0.7s cubic-bezier(0.34, 1.56, 0.64, 1);
}

.logo-container {
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: var(--spacing-lg);
}

.logo-header.shrunk {
    height: 80px;
    background: var(--glass-bg);
    pointer-events: auto;
}

.logo-header.shrunk .logo-container i {
    font-size: clamp(2rem, 6vw, 2.8rem);
}

.logo-header.shrunk .logo-container h1 {
    font-size: clamp(1.4rem, 4vw, 2rem);
    margin: 0;
}

.logo-container i {
    font-size: clamp(6rem, 18vw, 10rem);
    color: white;
    filter: drop-shadow(0 12px 32px rgba(0,0,0,0.5));
}

.logo-container h1 {
    font-size: clamp(4rem, 12vw, 6.5rem);
    background: linear-gradient(45deg, #ffffff 0%, transparent 80%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    font-weight: 900;
    letter-spacing: -0.03em;
    text-align: center;
    margin: 0;
}

/* Navbar - Floating Pills */
.navbar {
    display: flex;
    justify-content: center;
    align-items: center;
    padding: var(--spacing-sm) clamp(1.5rem, 6vw, 4rem);
    position: sticky;
    top: 0;
    z-index: 1000;
    background: none;
    gap: var(--spacing-xs);
    flex-wrap: wrap;
}

.navbar ul {
    display: flex;
    list-style: none;
    gap: var(--spacing-xs);
    background: var(--glass-bg);
    backdrop-filter: blur(15px);
    border: 1px solid var(--glass-border);
    border-radius: 50px;
    padding: 0.75rem 1.5rem;
    box-shadow: var(--shadow-sm);
    flex-wrap: wrap;
    justify-content: center;
    margin: 0;
}

.navbar a {
    color: white;
    text-decoration: none;
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    font-weight: 600;
    font-size: clamp(0.9rem, 2.5vw, 1.1rem);
    transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    white-space: nowrap;
}

.navbar a:hover, .navbar a.active {
    background: rgba(255,255,255,0.25);
    transform: translateY(-3px) scale(1.05);
    box-shadow: var(--shadow-md);
}

/* Page Hero */
.page-hero {
    padding: var(--spacing-lg) var(--spacing-md);
    text-align: center;
    color: white;
    max-width: 1400px;
    margin: 0 auto;
}

.page-hero h2 {
    font-size: var(--font-size-xl);
    margin-bottom: var(--spacing-md);
    text-shadow: 0 8px 32px rgba(0,0,0,0.4);
}

.hero-buttons {
    display: flex;
    gap: var(--spacing-sm);
    justify-content: center;
    flex-wrap: wrap;
    margin-top: var(--spacing-lg);
}

/* Cards */
.results-grid, .solution-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: var(--spacing-md);
    max-width: 1400px;
    margin: var(--spacing-md) auto;
    padding: 0 var(--spacing-md);
}

.card, .item {
    background: var(--glass-card);
    backdrop-filter: blur(20px);
    border: 1px solid var(--glass-border);
    padding: 2.5rem;
    border-radius: var(--border-radius);
    text-align: center;
    box-shadow: var(--shadow-sm);
    transition: all 0.5s cubic-bezier(0.34, 1.56, 0.64, 1);
    position: relative;
    overflow: hidden;
}

.card:hover, .item:hover {
    transform: translateY(-15px) rotateX(8deg);
    box-shadow: var(--shadow-lg);
}

.hero-icon {
    font-size: 3.5rem !important;
    color: var(--color-accent) !important;
    margin-bottom: var(--spacing-md);
}

/* Forms */
input, select, button {
    font-family: inherit;
    border-radius: 16px;
    border: none;
    transition: all 0.3s;
}

input, select {
    background: var(--glass-bg);
    color: var(--color-text-primary);
    padding: 1.25rem;
    width: 100%;
    margin-bottom: var(--spacing-sm);
}

button {
    background: var(--color-bg-secondary);
    color: white;
    padding: 1.25rem 2.5rem;
    cursor: pointer;
    font-weight: 600;
    box-shadow: var(--shadow-sm);
}

button:hover {
    transform: translateY(-4px);
    box-shadow: var(--shadow-lg);
}

/* Login Tab Styles */
.tab-nav {
    display: flex;
    gap: 1rem;
    margin-bottom: var(--spacing-md);
    background: var(--glass-bg);
    border-radius: var(--border-radius);
    padding: 0.5rem;
    backdrop-filter: blur(10px);
}

.tab-btn {
    flex: 1;
    padding: 1rem;
    border: none;
    border-radius: 12px;
    background: transparent;
    color: var(--color-text-secondary);
    cursor: pointer;
    font-weight: 600;
    transition: all 0.3s;
}

.tab-btn.active {
    background: var(--color-accent);
    color: white;
    box-shadow: var(--shadow-sm);
}

.tab-content {
    display: none;
}

.tab-content.active {
    display: block;
}

/* Responsive */
@media (max-width: 768px) {
    .navbar ul {
        flex-direction: column;
        border-radius: 20px;
        padding: 1rem;
    }
    
    .hero-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .tab-nav {
        flex-direction: column;
    }
}

/* Animations */
@keyframes float {
    0%, 100% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
}

.card:hover {
    animation: float 2s ease-in-out infinite;
}

